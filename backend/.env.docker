# Docker 环境示例（用于 docker-compose env_file）
# 说明：不建议把真实密钥提交到仓库；这里仅放默认配置。

PROJECT_NAME=自媒体自动化
ENV=docker

# Docker 下 uploads 持久化目录（docker-compose 会挂载 volume）
UPLOADS_DIR=/app/uploads

# Celery：Docker 默认走真异步（会启动 worker 容器）
CELERY_ALWAYS_EAGER=false

# 默认不启用定时任务（需要时用 beat profile + 打开开关）
DAILY_HOTSPOT_BEAT_ENABLED=false
MORNING_BRIEF_ENABLED=false

# 模型 Provider 默认值（你也可以在系统的 APIKey 池里配置）
DEFAULT_MODEL_PROVIDER=deepseek
MODEL_DEEPSEEK_VERIFY=true
